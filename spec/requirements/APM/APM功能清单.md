| 功能模块 | 功能项 | 功能说明 |
| --- | --- | --- |
| 集成 | 契约状态 | 本清单是产品能力盘点，不是实现验收入口；集成能力以 `specs/changes/apm-nats-vt-pipeline/spec.md`、ADR 0006/0008 和当前代码为准。旧接入源、Bearer Token 与 APM Gateway 方案已撤销。 |
| 集成 | 集成菜单结构 | 集成菜单下设添加接入、应用管理和接入列表；添加接入即时生成无状态配置，不创建接入源或凭据；接入列表按遥测自动发现的运行实例呈现。 |
| 集成 | 添加接入 / 接入方式总览 | SDK 组当前开放 Node.js、Java、Python 和 Go；.NET、自建 OTel Collector、OBI 与 OTel Operator 显示为未开放。Go 明确为手动 SDK，其余已开放语言提供自动探针入口。 |
| 集成 | 接入详情页布局 | 采用"① 上报端点 + ② 接入配置"两步垂直布局,两段同时可见、自上而下排布,**不使用 Tabs 切换**;**不展示**接入自检(传输与准入 / 数据落库 / 最近活跃 / 采样保留 4 项) / Trace 接入任务路径 5 步 / "接通后先核对" / "跨服务不断链"双栏静态提示 / 排错说明(Markdown 文档);自检与排错由"服务 / 探索"菜单基于真实数据承载。 |
| 集成 | ① 上报端点 | 选择有权访问的 APM 应用与云区域；Server 从 NodeMgmt 受信配置解析区域代理地址并只读返回 `http://<receiver_host>:4318/v1/traces`。客户端不能提交 endpoint，不创建 Token。 |
| 集成 | 端点 URL 映射 | SDK/Agent 通过受信区域内网访问代理 4318，再进入区域 Collector；`/telegraf/api` 只属于 Monitor，4317 仅作手工兼容。公网和其他不受信网络接入当前不支持。 |
| 集成 | ② 接入配置 / SDK | Node.js、Java、Python 提供 Host、Docker 与 Kubernetes 配置；Kubernetes 使用 Downward API Pod UID。Go 提供完整手动 SDK Provider 示例，不宣称零代码自动探针。所有配置包含标准 OTEL 属性和动态 `service.instance.id`，不含 Authorization Header。 |
| 集成 | ② 接入配置 / 后续方式 | .NET 自动探针、自建 OTel Collector、OBI 和 OTel Operator 暂未开放；未开放卡片不进入伪配置流程。 |
| 集成 | 接入列表 / 列表字段 | 一行一个 `service.instance.id`；列为服务 / 应用(namespace) / 环境 / 实例 ID / 组织 / 首次发现 / 最近上报 / 状态 / 操作。服务链接进入聚合详情，实例查看进入实例明细。 |
| 集成 | 接入列表 / 顶部工具栏 | 通过服务端有界分页按应用、环境、状态、时间和关键字筛选；默认只请求活跃实例，归档和历史由用户显式选择，不在浏览器一次加载全部实例。 |
| 集成 | 接入列表 / 状态判定与接入时间 | 实例活跃 = 近 15 分钟有 Span;静默 = 无活跃且不足 7 天;已归档 = 静默至少 7 天或手动归档。接入时间和最近上报按实例键计算,不复用服务状态。 |
| 集成 | 接入列表 / 行操作 | 支持查看实例明细和按 Operate 权限调整实例组织、归档/解档;服务链接另行进入逻辑服务详情。实例权限调整不改变服务全局指标。 |
| 集成 | 接入列表 / 跟服务目录数据一致性 | 接入列表展示运行实例,服务目录展示按 `service.namespace + service.name` 聚合的逻辑服务,二者不是相同数据行集合。服务详情的实例分布可下钻到相同实例记录。 |
| 服务目录 | 服务健康列表 | 自动发现并展示当前租户下全部被监控服务，包括：服务名、环境、版本、语言/运行时、吞吐、错误率、P99 时延、依赖服务数、健康状态、SLO 摘要，帮助客户快速识别异常服务。 |
| 服务目录 | 服务筛选 | 支持按环境、关键字、健康状态、版本、语言/运行时、标签筛选服务，帮助客户在多服务场景下快速定位目标服务。 |
| 服务目录 | 服务排序 | 支持按错误率、P99 时延、吞吐、健康严重度排序服务，帮助客户优先关注影响大或风险高的服务。 |
| 服务目录 | 服务健康分级 | 基于服务错误率和时延指标对服务进行健康、警告、严重、待定、陈旧/失联等状态分级，帮助客户识别服务是否处于正常、退化或停报状态。 |
| 服务目录 | 服务收藏 | 支持收藏常用服务，并在列表中置顶或独立展示，便于客户持续关注核心服务。 |
| 服务目录 | 单服务 RED 时序 | 在单服务详情中展示吞吐、错误率/错误量、时延三类 RED 指标时序，其中时延支持 P50、P75、P90、P95、P99 和平均值，帮助客户分析服务性能与稳定性趋势。 |
| 服务目录 | Top 端点 | 在单服务详情中展示贡献最大的端点，包括吞吐、错误率、P95/P99 时延、总耗时占比，帮助客户定位服务内主要问题入口。 |
| 服务目录 | Top 错误 | 在单服务详情中展示主要错误项及影响量，帮助客户快速识别当前服务最需要处理的错误。 |
| 服务目录 | 上下游依赖 | 展示单服务的上游调用方与下游依赖清单，并展示每条依赖的 RED 摘要，帮助客户理解服务调用关系和故障传播路径。 |
| 服务目录 | 部署事件叠加 | 在服务时序图上叠加版本或部署事件标记，包括版本号、部署时间等信息，帮助客户判断质量变化是否与发布相关。 |
| 服务目录 | 实例分布 | 按主机、Pod、容器、实例等维度展示服务实例及其 RED 摘要，帮助客户识别单实例异常。 |
| 服务目录 | 运行时指标 | 在支持语言下展示 JVM 堆、GC、线程、Go 堆、协程等运行时遥测数据，帮助客户分析运行时资源与性能问题。 |
| 服务目录 | Apdex 满意度 | 展示 0 到 1 的 Apdex 单值指标，并支持租户级和服务级阈值配置，帮助客户用统一口径衡量响应速度满意度。 |
| Trace 检索与详情 | 多维结构化检索 | 支持按服务、环境、版本、入口资源/操作、状态、最小/最大耗时、SpanKind、时间窗组合检索 Trace，帮助客户快速筛选慢请求、错误请求或指定业务链路。 |
| Trace 检索与详情 | 分面栏与查询框 | 提供分面点选和 `key:value` 查询框两种检索方式，并支持自动输入建议和检索条件同步，满足普通用户与熟练用户的不同查询习惯。 |
| Trace 检索与详情 | Trace 结构检索 | 支持按整条 Trace 的结构特征筛选，包括是否包含数据库调用、是否包含错误 Span、Span 数量、总耗时、跨服务数等，帮助客户发现结构异常的请求。 |
| Trace 检索与详情 | Span 与资源属性检索 | 支持按任意 Span 属性或资源属性过滤，包括等于、不等于、大于等于、小于等于、包含、集合匹配等方式，帮助客户基于 HTTP、数据库、主机、自定义业务标签等维度定位请求。 |
| Trace 检索与详情 | Trace 结果列表 | 以一条 Trace 一行展示检索结果，包括入口服务、入口资源、总耗时、Span 数、状态、开始时间，并支持按总耗时、开始时间、状态排序。 |
| Trace 检索与详情 | Span 结果视角 | 支持在 Traces 视角和 Spans 视角之间切换，Spans 视角展示所属服务、操作名、SpanKind、HTTP 方法与状态码、自身耗时、状态、发生时间，并可定位到所属 Trace。 |
| Trace 检索与详情 | 时延散点与分布观测 | 展示 Trace 时延散点图和按桶分布，支持在图上框选时间和耗时区间并回填检索条件，帮助客户定位长尾慢请求样本。 |
| Trace 检索与详情 | 聚合分析 | 支持按服务、入口资源、状态、版本、环境、主机等维度分组聚合，并展示吞吐、错误率、P50、P75、P90、P95、P99、平均耗时，帮助客户从样本检索上升到群体分析。 |
| Trace 检索与详情 | 可视化切换 | 支持在列表、时序、Top 榜、聚合表格等视图之间切换，帮助客户从不同视角分析同一组 Trace 检索结果。 |
| Trace 检索与详情 | 实时尾随 | 支持 Live 模式自动刷新最新 Trace 数据，并展示当前吞吐量和命中数，帮助客户观察正在发生的请求异常。 |
| Trace 检索与详情 | 保存检索视图 | 支持将检索条件、聚合设置、时间窗偏好保存为个人视图，并支持通过链接分享完整检索状态，方便客户复用常见排障查询。 |
| Trace 检索与详情 | Span 瀑布图 | 在单条 Trace 详情中以瀑布图展示所有 Span 的父子关系、开始偏移、持续时间、自身耗时和等待下游耗时，帮助客户还原一次请求的完整调用过程。 |
| Trace 检索与详情 | 火焰图 | 支持将单条 Trace 切换为火焰图，按调用栈层级展示各 Span 耗时占比，并与瀑布图选中状态联动，帮助客户识别耗时集中位置。 |
| Trace 检索与详情 | 关键路径高亮 | 自动高亮决定 Trace 总耗时的最长 Span 链，并支持只查看关键路径，帮助客户判断优化哪个调用段最能缩短总耗时。 |
| Trace 检索与详情 | 错误 Span 标注 | 对状态为错误的 Span 进行醒目标注，支持跳到首个出错 Span，并展示错误状态文本和异常事件，帮助客户快速定位请求失败位置。 |
| Trace 检索与详情 | Span 详情面板 | 展示选中 Span 的基本信息、计时信息、状态、属性、事件、异常栈和数据库查询信息，包括操作名、服务、版本、环境、SpanKind、主机/实例、耗时、状态文本、HTTP/数据库/RPC/消息属性等。 |
| Trace 检索与详情 | Span Links | 支持展示跨 Trace 的 Span Links，并可跳转到被链接的 Trace 或 Span，帮助客户分析消息队列、批处理、异步任务等非父子调用关系。 |
| Trace 检索与详情 | 跨服务边界与断裂标注 | 在瀑布图和火焰图中区分跨服务边界、网络等待段和缺失 Span，并对可能受采样或未插桩影响的不完整链路进行提示，帮助客户正确理解调用链。 |
| Trace 检索与详情 | 超长 Trace 展示优化 | 支持重复子树折叠、短 Span 过滤、长列表虚拟滚动和超限提示，帮助客户在 Span 数量很大的 Trace 中保持可读性。 |
| 端点 | 端点级 RED 时序 | 按服务内端点维度展示吞吐、错误率、时延三联时序，其中时延支持 P50、P75、P90、P95、P99 和平均值，帮助客户定位具体接口或操作的性能问题。 |
| 端点 | 时延分布 | 提供时延直方图或热力图，展示不同时间桶和耗时桶的 Span 数，并展示 P50、P75、P90、P95、P99、平均、最大值、样本量，帮助客户识别双峰和长尾问题。 |
| 端点 | 端点健康判级 | 基于端点 RED 指标对端点进行健康、警告、严重分级，并在低流量场景提供样本量提示，帮助客户判断端点是否真实退化。 |
| 端点 | 状态码细分 | 按 HTTP 2xx、3xx、4xx、5xx 及具体状态码拆分占比与趋势，同时支持按 Span 状态分组，帮助客户区分服务端错误、客户端错误和正常请求。 |
| 端点 | 时延构成与下游分解 | 展示端点调用过程中的下游依赖，包括数据库、缓存、其他服务、外部 API 的调用次数、时延、错误率以及占本端点总时延比例，并区分自身耗时、等待下游耗时和未归因耗时。 |
| 端点 | 端点归并与折叠 | 支持 HTTP 路由模板化、数据库语句参数化与脱敏、消息目标或 RPC 方法归一化，并对缺少模板的端点进行降级归并，帮助客户避免高基数端点膨胀。 |
| 端点 | 端点列表与排序 | 在指定服务下展示所有端点，并支持按总耗时贡献、错误率、P95/P99 时延、吞吐排序，帮助客户优先选择最值得优化的接口。 |
| 端点 | 跨维度切片 | 支持按环境、版本、实例等维度切片查看端点 RED 指标，帮助客户判断问题是否集中在某个版本或实例。 |
| 端点 | 样本 Trace 下钻 | 提供归属本端点的样本 Trace 列表，支持仅慢、仅错、最小时延阈值等过滤，并可从时序点、分位、状态码桶、耗时区间下钻到 Trace 详情。 |
| 端点 | 端点元信息 | 展示端点归一化标识、HTTP method、SpanKind、典型对端服务、脱敏后的语句模板、所属服务、环境和当前过滤条件，帮助客户理解端点统计口径。 |
| 服务拓扑 | 服务依赖有向图 | 将租户内服务调用关系渲染为有向图，其中节点代表服务或推断依赖，边代表调用关系，方向为上游调用下游，帮助客户建立系统调用结构视图。 |
| 服务拓扑 | 节点健康与吞吐编码 | 通过节点颜色展示健康状态，通过节点大小展示吞吐，并在悬停时展示服务吞吐、错误率、P50/P95/P99 时延等 RED 摘要，帮助客户快速识别异常服务和高流量枢纽。 |
| 服务拓扑 | 边指标标注 | 在调用边上展示或悬停查看请求速率、错误率、P99 时延等指标，并通过边粗细表达吞吐、边颜色表达健康，帮助客户识别异常调用关系。 |
| 服务拓扑 | 时间窗拓扑 | 支持按最近 15 分钟、1 小时、4 小时、1 天、7 天或自定义时间窗重算拓扑节点、边和健康指标，帮助客户对比故障前后服务依赖变化。 |
| 服务拓扑 | 环境过滤 | 支持按生产、预发、开发等环境单选过滤拓扑，避免不同环境调用关系混合展示。 |
| 服务拓扑 | 节点下钻 | 支持点击拓扑节点查看服务 RED 摘要、上下游清单，并跳转到对应服务详情，帮助客户从宏观拓扑进入单服务分析。 |
| 服务拓扑 | 边下钻 | 支持点击调用边查看上游到下游的调用 RED 指标，并携带上游服务、下游服务、时间窗、环境等条件跳转到 Trace 检索。 |
| 服务拓扑 | 上下游聚焦 | 支持选择节点进入 Ego 图聚焦模式，仅展示该节点及 N 跳上下游邻居，帮助客户在大规模拓扑中专注分析局部调用链。 |
| 服务拓扑 | 搜索与定位 | 支持按服务名模糊搜索节点，匹配后高亮并自动居中，帮助客户快速定位目标服务。 |
| 服务拓扑 | 布局与交互 | 支持缩放、平移、适配全图、重置视图、力导向布局、分层布局和节点位置固定，提升复杂拓扑的可读性。 |
| 服务拓扑 | 分组聚合与折叠 | 支持按命名空间、团队、标签等维度分组折叠节点，展示组级 RED 汇总和跨组调用边，并支持展开查看组内子图，帮助客户管理上百服务规模的拓扑。 |
| 服务拓扑 | 视图分享 | 支持将时间窗、环境、聚焦节点、跳数、布局、搜索高亮等视图状态固化为分享链接，便于客户协同排障。 |
| 服务拓扑 | 环路与自调用识别 | 支持识别并标注服务自调用和循环依赖，帮助客户发现架构耦合和潜在风险。 |
| 错误追踪 | Issue 自动聚类 | 将状态为 Error 的 Span 和 Span 上的 exception 异常事件自动归一化,并按异常类型、归一化消息、关键栈帧、服务等维度聚类为 Issue,帮助客户把大量同源错误折叠为可管理的问题。 |
| 错误追踪 | Issue 列表 | 顶部工具栏(时间 Radio + 服务 Select + 环境 Select)+ 概览 KPI 条(异常 Issue / 出现次数 / 受影响 trace / 受影响服务)+ 卡片列表(服务名 + 受影响版本 tag + 受影响端点 tag + Issue 类型 + 消息 + 受影响 Trace 数 / 出现次数 / 最近出现三列数据 + 折叠区含完整堆栈 / 版本分布 / 端点分布);**不展示**处理人、状态切换入口、tag chips(全部 / 新增 / 回归 / 激增等)。 |
| 错误追踪 | Issue 筛选 | 顶部工具栏仅支持按时间窗(`15m` / `1h` / `4h` / `1d` / `7d`)+ 服务 + 环境筛选 Issue;**不展示**认领、标签、生产环境隔离等快捷分面(后端能力保留,第一版 UI 暂不暴露)。 |
| 错误追踪 | 典型样本 Trace | 通过 Issue 卡片"受影响 Trace 数"入口跳转到该 Issue 的样本 Trace 列表(走 TracesSearch 预填检索条件),并下钻到单条 Trace 瀑布图,帮助客户复现错误上下文。 |
| 错误追踪 | 聚合异常栈 | 卡片折叠区展示该 Issue 的代表性异常堆栈,完整多行 mono 字体显示,帮助客户定位错误发生位置。 |
| 错误追踪 | 版本分布 | 卡片折叠区按版本展示 Issue 的次数与占比,帮助客户判断错误是否由某次发布引入。 |
| 错误追踪 | 端点分布 | 卡片折叠区按端点或操作名展示 Issue 命中次数与占比,帮助客户定位错误集中在哪些接口。 |
| 错误追踪 | Issue 生命周期(后端) | 后端保留状态机:**待分诊 → 已分诊 → 已解决 / 已排除**,并记录操作人与时间;**不设**已忽略 / 静音 / 降噪状态;第一版 UI **不展示**状态切换入口,用户对 Issue 的处置仅通过"已分诊 / 已解决 / 已排除"三种主动操作完成(后续版本按需开放 UI)。 |
| 错误追踪 | 回归识别(后端) | 已解决 Issue 在近窗(默认 7 天)再次出现时自动重开为待分诊并标记 Regression(后端能力);时间窗可在系统配置中调整;回归识别按聚类键严格匹配,跨版本同根因需通过"自定义归并规则"手动合并;第一版 UI 不展示该标记。 |
| 错误追踪 | Issue 归并与拆分(后端) | 支持人工合并误拆 Issue、按消息骨架 / 关键栈帧 / 端点拆分混合 Issue,并支持自定义归并或拆分规则,帮助客户校正错误聚类口径(后端能力,第一版 UI 不暴露)。 |
| 错误追踪 | 激增标记(后端) | 当单个 Issue 在短时间内次数陡增时自动标记 Spiking(后端能力,第一版 UI 不展示),帮助客户识别正在爆发的根因错误。 |
| 错误追踪 | 错误告警接入 | 支持将新 Issue、回归、激增、受影响 Trace 数或影响面超阈值作为告警触发源,并复用告警中心的通知路由、静默和生命周期能力(P1 阶段能力,不在 P0 验收范围)。 |
| SLO | SLO 列表 | 展示租户内全部 SLO，包括服务或端点、目标、窗口、当前达成率、剩余错误预算、燃尽率、健康状态，帮助客户总览可靠性目标达成情况。 |
| SLO | SLI 定义 | 支持基于入口 Span 定义可用性 SLI 和延迟达标率 SLI，其中可用性以非错误入口 Span 为 good，延迟以耗时小于配置阈值的入口 Span 为 good。 |
| SLO | SLO 配置 | 支持配置服务级或端点级 SLO，包括 SLI 类型、目标达成率、滚动或日历窗口、窗口长度、good 口径，帮助客户建立可靠性契约。 |
| SLO | 时间窗口 | 支持滚动窗口和日历窗口，滚动窗口持续回看最近 N 天，日历窗口按自然月或季度结算，满足实时治理和周期汇报两类场景。 |
| SLO | 错误预算 | 自动计算错误预算总额、已消耗预算、剩余预算和燃尽率，并在无数据或样本不足时不做达标/违约结论，帮助客户正确评估可靠性风险。 |
| SLO | 燃尽率告警 | 支持基于错误预算燃尽率进行快慢双窗口告警，例如短窗高倍率和长窗中倍率，帮助客户在彻底违约前提前发现可靠性风险。 |
| SLO | 状态总览 | 提供 SLO 总览看板，支持按剩余预算、燃尽率、违约状态排序，并按服务、环境、负责团队、窗口类型、是否违约筛选或分组。 |
| SLO | 历史合规与趋势 | 展示历史达成率时序、目标线、按结算周期的合规台账，并支持导出用于复盘、汇报或对客 SLA 沟通。 |
| SLO | good 口径可配 | 支持配置客户端 4xx 是否计入失败、主动取消或限流降级是否合格/不合格/排除出分母，并在 SLI 配置和 SLO 详情中展示 good/total 精确定义。 |
| 部署追踪 | 部署事件识别 | 支持通过 service.version 首次出现自动推断部署事件，也支持 CI/CD 外部上报版本、环境、提交标识、部署人、变更链接、起止时间等结构化部署信息。 |
| 部署追踪 | 部署标线叠加 | 在服务详情、端点、拓扑相关时序图上叠加部署竖线、旗标或混合窗口区间，悬停展示版本、环境、提交标识、部署人、部署时刻和变更链接。 |
| 部署追踪 | 部署列表 | 按服务和环境展示部署事件列表，包括版本、环境、部署时刻、来源、提交标识、部署人、版本 RED 摘要、相对上一版本 delta、回归标记等信息。 |
| 部署追踪 | 单部署详情 | 展示单次部署的基本信息、版本生命周期 RED 时序与汇总、基线版本对比、本次引入或消除的 Issue、慢/错样本 Trace 入口，帮助客户评估一次上线的质量影响。 |
| 部署追踪 | 版本前后对比 | 支持选择两个版本并排对比吞吐、错误率、P50、P75、P90、P95、P99、平均耗时，并展示绝对值、相对百分比、改善/持平/恶化方向。 |
| 部署追踪 | 端点级 delta | 支持将版本前后质量变化下钻到端点维度，帮助客户判断回归是整体退化还是个别端点拖累。 |
| 部署追踪 | 回归检测 | 当新版本相对基线在错误率或时延分位上显著恶化且样本量达标时，自动标记疑似回归，并可跳转到对比视图和代表性慢/错 Trace。 |
| 部署追踪 | 滚动发布识别 | 支持识别新旧版本共存的混合窗口，并在版本对比和回归判定中排除混合窗口，帮助客户避免灰度或滚动发布期间的误判。 |
| 部署追踪 | 回滚识别 | 支持识别版本号回退到旧版本的情况，并记录为新的回退部署事件，帮助客户追踪回滚后的服务表现。 |
| 部署追踪 | 缺陷引入与消除 | 在部署详情中展示本次部署引入的 Issue 和本次部署后消除的 Issue，并与错误追踪中的首现版本、消除版本、回归状态联动。 |
| 部署追踪 | 版本与环境过滤 | 在服务详情、端点、Trace 检索、错误追踪、拓扑等视图中支持按环境和版本过滤，并将过滤状态写入分享链接，帮助客户避免不同环境或版本混看。 |
| 采样与保留 | 采样规则列表 | 展示头采样比例、尾采样策略、作用范围、来源层等规则信息，帮助客户了解当前 Trace 数据进入存储链路的策略。 |
| 采样与保留 | 头采样 | 支持按服务、环境配置入口概率采样，并沿调用链一致传播采样决策，帮助客户对平凡高频流量进行粗筛降量。 |
| 采样与保留 | 尾采样 | 支持在整条 Trace 收齐后按条件保留，包括含错误强制保留、超慢阈值强制保留、属性匹配保留、兜底概率保留，帮助客户优先保留高诊断价值 Trace。 |
| 采样与保留 | 采样差异化矩阵 | 按服务和环境展示当前生效的采样规则及来源，并支持平台默认、环境级、服务级逐级覆盖，帮助客户按业务重要性治理采样。 |
| 采样与保留 | 保留分层 | 支持对错误/慢 Trace 和普通 Trace 采用不同留存时长档位，在统一保留窗口内优先延长高价值 Trace 的可用时间。 |
| 采样与保留 | 用量看板 | 展示摄入量、存储占用、按服务/环境归因、采样规则命中量和实际生效采样率，帮助客户理解成本来源和数据量分布。 |
| 采样与保留 | 配额与超额策略 | 支持设置摄入量和存储占用软配额、预警阈值，并在超额后选择自动降采样或丢弃溢出平凡 Trace，同时优先保留错误和慢 Trace。 |
| 采样与保留 | 采样可解释性 | 当客户检索不到预期 Trace 时，支持区分确实未发生、被采样丢弃、超出保留期、属性未索引等原因，帮助客户正确理解数据可得性。 |
| Span 指标 | 指标列表 | 展示租户内全部 Span 指标定义，包括名称、度量/聚合摘要、过滤器、分组概览、启停状态，帮助客户管理自定义指标口径。 |
| Span 指标 | 指标编辑器 | 支持用过滤器、度量、聚合、分组四要素定义 Span 指标，过滤器可按服务、Span 名/端点、环境、是否仅错误 Span、Span 属性等条件限定计算范围。 |
| Span 指标 | 度量与聚合 | 支持计数、Span 耗时、数值属性等度量，并支持计数、求和、平均、最小、最大、P50、P75、P90、P95、P99 等聚合方式，帮助客户构建业务或性能切片指标。 |
| Span 指标 | 分组标签 | 支持按 Span 属性键拆分多条指标序列，并对高基数分组数量进行限制，帮助客户在可控范围内分析不同维度表现。 |
| Span 指标 | 实时预览 | 在编辑指标时实时预览近窗时序结果，并在分组序列过多时展示 Top-N 和截断提示，帮助客户保存前确认指标口径。 |
| Span 指标 | 定义管理 | 支持创建、编辑、启停、删除 Span 指标定义，并区分管理权限和只读权限，帮助客户治理自定义指标生命周期。 |
| 性能建议 | 建议总览 | 展示待审可靠性问题数和待审性能问题数，帮助客户快速了解当前需要关注的优化项规模。 |
| 性能建议 | 建议列表 | 按优先级评分降序展示建议清单，包括问题类型、服务/资源、判据数字、建议状态，帮助客户优先处理最有价值的问题。 |
| 性能建议 | 建议筛选 | 支持按规则类型、状态、服务筛选建议，其中规则类型包括高异常量、慢 DB 调用，帮助客户聚焦特定服务或问题类型。 |
| 性能建议 | 高异常量识别 | 自动识别服务端点错误率超过阈值且请求量达到下限的场景，并引导客户下钻错误 Trace 样本定位下游故障、入参异常或超时问题。 |
| 性能建议 | 慢 DB 调用识别 | 自动识别数据库操作 P95 自身耗时超过阈值且调用量达到下限的场景，并建议检查执行计划、索引、返回列/行集、缓存或读写分离。 |
| 性能建议 | 优先级评分 | 基于影响程度和请求量/触发量计算可解释评分，并展示影响因子和量级因子，帮助客户理解建议排序原因。 |
| 性能建议 | 三段式详情 | 每条建议按问题、影响、建议三段展示，其中问题包含服务/资源和判据数字，影响说明对延迟、可用性或成本的影响，建议提供排查和修复方向。 |
| 性能建议 | 建议状态管理 | 支持将建议标记为待审、已忽略、已解决或重新待审，并按问题指纹持久化状态，帮助客户形成优化项治理闭环。 |
