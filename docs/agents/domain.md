# 领域文档约定

- `CONTEXT.md`：共享语言和术语。
- `docs/adr/`：难以回滚、存在真实取舍的长期决定。
- `specs/capabilities/`：长期业务语义、验收、架构边界和运行约束。
- `specs/changes/`：跨会话变更 spec 与必要 tickets。

不要在 glossary 或 ADR 中复制 capability。代码符号、表字段、组件树和测试断言由代码维护。
