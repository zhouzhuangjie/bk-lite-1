# 运营分析 · 运营分析

> Migrated from `spec/prd/运营分析/运营分析.md` as legacy capability evidence.

## 1. 背景与目标

运营分析提供可视化分析视图，让用户以仪表板、拓扑、架构图、大屏、报表等形式自助编排和查看跨来源的运营数据。

## 2. 范围定义

- 视图类型：仪表板、拓扑、架构图、大屏、报表、网络拓扑。
- 视图组织：自定义目录组织视图。
- 过滤与命名空间：统一过滤条与命名空间选择。

## 3. 关键能力

- 支持六类视图，按自定义目录分层组织：
  - 仪表板：栅格化拖拽布局，组件类型以 widgetRegistry 注册为准（折线、柱状、饼图、表格、单值、Top-N、仪表盘、事件表、事件时间线、卡片列表、雷达、3D 机房、网络状态拓扑、多值、文本、拓扑地图，共 16 类），每个组件绑定数据源查询。
  - 拓扑：图形可视化，节点可展示单值或图表并实时刷新，支持节点与边。
  - 架构图：等距图编辑，可拖入云厂商、K8s、资产等图标设计系统拓扑。
  - 大屏：面向全屏展示的大画布，支持统一筛选、命名空间切换、组件布局与全屏模式。
  - 报表：报表视图支持按时间范围组织章节；当前为可编辑草稿（表格/事件表）、筛选、刷新、全屏、分享、PDF 与订阅，不是只读基础模式。
  - 网络拓扑：独立画布，对接 WeOps 连接与运行态链路/指标预览。
- 视图通过数据源（在管理中配置）取数，数据源可来自 NATS、数据库、REST API 或 Excel，查询支持固定参数、用户筛选与运行时参数。
- 提供统一过滤条，过滤项绑定组件查询并作用于整个视图；仅字符串、时间范围、日期范围参数可参与绑定。
- 提供命名空间选择，决定视图取数的来源连接。
- 支持编辑模式（增删/重排组件、改查询）与自动保存、未保存离开提醒。
- 仪表板支持可折叠分组：编辑态可新增、重命名、取消分组、删除整组，分组可整体拖拽排序与展开/收起；查看态保留展开/收起。
- 单值、Top-N、仪表盘组件支持周期对比：开启后自动对比上一个等长周期的数据并显示变化百分比；支持按阈值区间为数值/指针配色。
- 事件表（事件流）组件按配置展示字段列表，支持分页与行展开查看明细。
- 网络状态拓扑组件支持按模型、实例与深度参数请求网络状态拓扑数据，并在画布中渲染拓扑关系；编辑态可在层级、力导向与环形布局之间切换，各布局的节点和连线编辑结果分别保存，并可只重置当前布局；查看与分享不写回配置。
- 3D 机房组件消费 CMDB NATS `get_room3d_layout` 返回的机房布局数据，渲染 row/col 网格、机柜 U 占用、设备摘要与类型名图例；机柜类型同时返回 `datacenter_type` 枚举 id 与可读名称（如「计算」「网络」「存储」等），无值时仅返回 id。
- 三类视图均支持全屏查看；拓扑额外支持大屏配置：设定固定分辨率（宽、高）与大屏背景色（信箱填充色），并将该视口随视图一同持久化、支持导入导出。

### 3.1 画布刷新

仪表盘、拓扑、大屏和网络拓扑画布支持关闭、1 分钟、5 分钟、10 分钟四档刷新频率。页面隐藏时暂停周期刷新；重新可见后静默刷新。静默刷新失败时保留已展示的成功数据，不以失败状态覆盖画布。

> 证据来源：server/apps/operation_analysis/constants/canvas_refresh.py:6-30，server/apps/operation_analysis/models/models.py:85-112,145-153,188-210，web/src/app/ops-analysis/hooks/useCanvasPeriodicRefresh.ts:69-109，web/src/app/ops-analysis/utils/canvasRefreshTimer.ts:40-52,124-134　|　同步基线：b98b782a7　|　【已实现】

### 3.2 场景组件与值映射

网络状态拓扑组件仅接受 CMDB canonical UUID v4 作为实例标识，不回退数字 ID。值映射规则可分别配置展示文字和颜色；未配置的结果维度保持原有展示或颜色。

> 证据来源：web/src/app/ops-analysis/utils/cmdbInstanceUuid.ts:1-5，web/src/app/ops-analysis/components/widgetConfig/hooks/useNetworkStatusTopologyConfig.ts:34-47，web/src/app/ops-analysis/components/widgets/networkStatusTopology/index.tsx:240-252，web/src/app/ops-analysis/utils/valueMapping.ts:16-40　|　同步基线：b98b782a7　|　【已实现】

## 4. 关键规则

- 视图按组织分组，用户仅可见所属组织的视图；可见性与组织归属挂钩，不按创建人或实例数据权限收缩。
- 内置视图对组织可见但常规用户不可删改。
- 组件不直接查询其他模块，统一经数据源取数；NATS 由命名空间路由，数据库 / REST API / Excel 由数据源连接配置直连。
- 仪表盘组件最大值必须大于最小值，否则配置不生效。
- 周期对比仅在数据源已配置可识别的时间范围参数时可开启，否则不可用。
- 事件表分页依赖数据源是否提供分页参数；未配置展示字段时组件不渲染数据。
- 拓扑大屏视口仅在同时填写宽与高时生效；背景色仅接受合法十六进制颜色，否则回退为黑色。
- 网络状态拓扑组件必须提供模型、实例与可选深度参数，通过专用场景组件接口取数，不复用普通数据源查询入口。
- 3D 机房组件消费 CMDB NATS `get_room3d_layout`，机柜类型 `datacenter_type` 同时返回枚举 id 与可读名称；机柜顶部贴图支持「位置 + 类型名」双行排版，图例渲染源为 `rack_type_name` 字符串。
- 网络状态拓扑的三种布局配置相互独立；重置只清除当前布局的人工调整，切换布局不覆盖其他布局的保存结果。
- 仪表盘组件允许配置为 dashboard/screen 两种 surface，不允许配置为 topology 表面（`OpsAnalysisWidgetSurface` 收紧为 `'dashboard' | 'screen'`）。
- 报表当前为基础模式，只读展示已保存内容，不提供完整在线编排器。

## 5. 关键技术架构选择

- 模块定位为"可视化 + 配置层"，本身不做复杂数据建模，取数全部下沉到用户定义的数据源。
- 取数按数据源类型分流：NATS 经命名空间路由到下游服务；数据库 / REST API / Excel 由数据源连接配置直接完成预览或查询；请求统一携带用户上下文用于鉴权。

相关架构：[[legacy-ard-modules-operation-analysis#3. 接口【已实现/已存在】]]；对应功能清单：[[legacy-fuctionlist-09-运营分析-功能清单#3. 仪表盘组件]]
> 证据来源：web/src/app/ops-analysis/utils/networkStatusTopologyLayout.ts:37-55,203-327，web/src/app/ops-analysis/components/widgets/networkStatusTopology/index.tsx:154-164,393-430,491-502，server/apps/operation_analysis/serializers/datasource_serializers.py:127-139　|　同步基线：d2769559　|　【已实现】
- 大屏与报表复用画布类 CRUD、权限与目录组织模型；网络状态拓扑走专用场景组件接口而非通用数据源接口。
- 视图配置支持 YAML 导入导出，并解析依赖（如仪表板带出其数据源）；拓扑导入导出含节点、边、过滤项与视口配置。

## 6. 验收标准

- 用户可创建并查看仪表板、拓扑、架构图、大屏、报表、网络拓扑六类视图。
- 组件可绑定数据源并按过滤条、命名空间取数。
- 视图按组织范围正确隔离，内置视图不可删改。
- 仪表板可创建可折叠分组并对组件进行分组、取消分组、删除整组，分组与组件均可拖拽排序。
- 仪表盘、单值、Top-N 组件可按阈值配色，开启周期对比后可显示较上周期的变化百分比。
- 事件表组件可按配置字段展示数据，支持分页与行展开查看明细。
- 网络状态拓扑组件可按模型、实例与深度参数请求并展示拓扑关系。
- 3D 机房组件可按机房实例参数请求并展示机柜 row/col 网格、U 占用、设备摘要与类型名图例。
- 三类视图可进入全屏；拓扑可设置固定分辨率与大屏背景色，并随视图导入导出保留。
