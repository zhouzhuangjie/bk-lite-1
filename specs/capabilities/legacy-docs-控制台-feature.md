# 功能介绍

> Migrated from `spec/docs/控制台/feature.md` as legacy capability evidence.

---
sidebar_position: 3
---

控制台模块旨在在全系统复杂的各类核心业务中寻找一个共治的交集锚点。在此大框架下，我们将其内部的主要机能拆解为了入口门户聚合、全局消息投递中心以及个人底座配置三大领域。

## 1. 入口门户区 (Home)
当使用者推开这道大门之后的首要交互区，负责完成资源聚合路由寻址调度。

*   **动态应用卡片展示**：后台在页面渲染前会自动关联并校验该员工所有关联组织内匹配下发的角色配置表，实现“所见即已有权限用”（有效解决新入职员工面对茫茫应用海，因为误撞点进无授权系统导致不必要受挫）。并且卡片支持自动提取最新的系统标识语言配置投射出来。
*   **个性化卡片排序**：支持用户通过直观的拖拽操作，灵活调整首页各功能卡片的排列顺序。个人偏好的入口顺序将自动同步云端永久留痕，显著提升每位员工日常高频访问的效率。
*   **空载场景首登引导器**：对真正意义上第一次访问系统且完全不具备任何底层历史配置属性存留的账号发起初始化的强制引导加载。

## 2. 消息服务中心 (Notification Center)
这是一个提供给全员的内生订阅接收信箱。各类关联服务事件状态与通报指令汇聚地。

*   **精细化分页查询过滤墙**：在数据层面采用由近至远倒序。不仅支持查询阅读整体规模，还可以精确对下发通告的对应子模块组件类别以及单一的“已读/未读状态”做筛查（帮助用户不遗漏任何有价值的重要系统消息节点）。
*   **无感知批量消费闭环**：除了正常的点对点展开事件阅览外。在消息呈现堆积时具备一键对通告标设全部已读或执行整条目强删除功能（保证了企业人员待办信息能够实现快速迭代消费和存储自释放）。

> **🖼️ 界面指引：**
>
> ![系统信息中心工作区](https://bklite.ai/docs/assets/console-notification-center.png)
>
> *   **状态逻辑**：该通知总线的生成机制为严格由后方 RPC 服务调用产生。您无需在界面上做主观撰写录入。任何进入此处的消息必定是在企业其他业务模块（例如您配置的故障停机发版）经由策略触达后形成的系统客体痕迹流转。

## 3. 用户信息中心 (User Profile)
支撑管理用户自身的核心凭证状态以及业务沟通偏好锚定。

*   **区域化偏好同步管控**：提供了全方位的语言（跨国化中英支持）与基础时区修正器（从而保证日志审计流在展示侧的绝对本地时间一致对齐）。
*   **邮箱双向校验环换流**：支持带有安全阻断验证发信手段的个人身份辅助联系邮箱改签绑定功能（防范由于身份盗用引发内部验证凭据等核心信息发配外联泄漏的风险）。
*   **带强策略校验的密码轮转库**：对向内修改提供基于整个大平台统一底座同步控制的安全基线配置约束（当且仅当输入的更换口令满足控制侧在安全规则要求的特殊数字、混合字符最低限度复杂度通过时才能落库生效并生效跨端使用）。

> **⚠️ 注意 / 安全最佳实践：**
> “密码、安全校验等操作变更仅针对于当前处于会话控制中的当事人有效”。因此如果您长期离开了该办公屏幕界面范围，建议养成主动切换下线锁屏退出的好习惯。如果您在此发生了口令更新行为，系统也会自动接管并废弃并强切除其他存留的（例如手机端APP或浏览器隐身窗口残照的）关联Token失效，所以届时请记得跨全端重新做一轮新的安全登录验证行为。
