# Mobile 运维微场景 MVP 实施计划

## 目的

本文把 [产品规格](./spec.md) 拆成可排期、可验证、可独立交付的工程任务。规格描述必须实现的用户行为；本文描述推荐的实施顺序、模块接口和完成门槛。

任务状态约定：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成并通过该任务验证
- `[!]` 因事实冲突暂停，等待产品确认

## 当前基线

- Mobile 当前一级壳只有 `apps | profile` 两个 Tab，实现在 `mobile/src/components/mobile-tab-shell/`。
- 登录成功和根路由当前固定进入 `/workbench`，分别位于 `mobile/src/context/auth.tsx` 与 `mobile/src/app/page.tsx`。
- 当前已有 `MobileNavigationProvider`、页面头、下拉刷新和主题模块，可扩展复用。
- `/conversation` 已有独立抽屉手势，必须保留为导航特例。
- Mobile 当前没有 Web 等价的应用许可与菜单权限解析模块。
- 语言已进入认证状态，但账号时区尚未形成统一状态和日期格式化入口。
- 告警、监控和 CMDB 在 Server/Web 已有能力；本计划默认复用，不默认新增数据库迁移。

## 模块设计

### 1. 平台可用性模块

在认证完成和业务页面之间建立一个深模块，隐藏许可、静态菜单、用户菜单、自定义菜单和路由回退的组合复杂度。

建议接口只暴露调用方真正需要的结果：

```ts
type MobileModuleKey = 'todo' | 'monitor' | 'assets' | 'apps' | 'profile';

interface MobileAvailability {
  status: 'loading' | 'ready' | 'error';
  visibleModules: MobileModuleKey[];
  canAccess(module: MobileModuleKey, capability?: string): boolean;
  resolveSafeRoot(preferred?: MobileModuleKey): string;
  refresh(): Promise<void>;
}
```

实现内部负责：

- 拉取和合并 Web 当前许可、静态菜单、用户菜单、自定义菜单事实；
- 复用 Web 的目录、父级、非菜单项及 operation 解析语义；
- 固定 Mobile Tab 顺序；
- 加载/失败时关闭业务入口；
- 按账号记忆最近一级 Tab；
- 失权时计算安全回退。

业务页面不得直接各自拼许可和菜单请求。生产环境使用 HTTP adapter，模块接口测试使用内存 adapter；不要再额外暴露传输层响应结构。

### 2. 账号偏好模块

建立统一账号偏好模块，向页面提供规范化后的语言、时区和时间格式化能力：

```ts
interface AccountPreferences {
  locale: string;
  timezone: string;
  formatDateTime(value: ApiDateValue, options?: DateTimeDisplayOptions): string;
  update(input: { locale?: string; timezone?: string }): Promise<void>;
  refresh(): Promise<void>;
}
```

- 语言和时区来自账号服务，并保留 Mobile 当前已有的语言和时区更新能力。
- 主题继续由现有本地主题模块负责，不得与账号偏好合并为同一种持久化。
- 时间解析、规范化和格式化在一个模块内完成，页面不再直接使用设备时区、硬编码 locale 或手写偏移。
- 更新成功后同步认证用户状态；更新失败不保留伪成功值。

### 3. 导航壳模块

`MobileTabShell` 只消费平台可用性模块给出的可见 Tab 和安全路由，不理解 Web 菜单树。

- 一级根页是返回栈边界。
- 详情页通过现有导航模块声明安全父页。
- 原生返回手势的启用规则集中在导航模块，不分散到页面。
- `/conversation` 明确保持禁用原生返回手势。

### 4. 四个业务模块

待办、监控、资产、智能应用各自拥有业务类型、请求 adapter、状态解析和页面。不得建立跨四个业务的“通用资源详情”或“通用列表仓库”；只有已有至少两个真实使用方的稳定触摸交互才进入共享模块。

## 路由建议

具体命名可在实现前按现有 Next.js App Router 约定微调，但父子关系不得改变：

| 模块 | 一级根页 | 子页 |
|---|---|---|
| 待办 | `/todo` | `/todo/search`、`/todo/alerts/[id]`、处理人选择浮层/页面 |
| 监控 | `/monitor` | `/monitor/objects/[objectId]`、`/monitor/objects/[objectId]/instances/[instanceId]` |
| 资产 | `/assets` | `/assets/search`、`/assets/models/[modelId]`、`/assets/models/[modelId]/instances/[instanceId]` |
| 智能应用 | `/workbench` | 保留 `/search`、`/workbench/detail`、`/conversations`、`/conversation` |
| 我的 | `/profile` | 保留并扩展 `/profile/accountDetails` |

动态段路由用于表达层级，不要求把业务对象缓存进导航壳。

## 阶段 0：事实核验门禁

目标：在开始页面开发前，证明现有 Web/Server 能力能够支撑规格。产出一张实现证据表，记录前端调用位置、服务端路由、请求字段、响应字段、权限点和最小样例。

- [x] `F0-1` 核验五个 Tab 的 client 名称、菜单 name/url、operation 和自定义菜单解析结果。
- [x] `F0-2` 核验 `/core/api/get_client/` 在社区版、企业许可有效/无效时的真实返回。
- [x] `F0-3` 核验告警三个列表筛选、详情、事件、操作记录、处理人范围及四个处置动作的现有接口；操作记录按产品确认继续沿用 Web 接口和 `operation_log-View`。
- [x] `F0-4` 核验告警级别排序可稳定使用 `level_id`，以及活动状态值与服务端枚举一致；级别读取按产品确认继续沿用 Web 接口和 `global_config-View`。
- [x] `F0-5` 核验监控对象、对象内实例搜索、有效插件、指标分组、瞬时值和区间值接口。
- [x] `F0-6` 核验 CMDB 分类、模型、模型计数、实例列表、字段分组、两阶段全文搜索接口。
- [x] `F0-7` 核验 `cmdb_followed_assets` 的 Web 读写结构、并发更新方式和当前权限行为。
- [x] `F0-8` 核验账号资料、组织/角色、显示名、语言、时区的 Web 字段和更新接口，并确认当前 Mobile 更新请求与 Web 一致。
- [x] `F0-9` 将核验结果写入本变更目录的 `evidence.md`；不得把抓包中的 token、Cookie 或真实敏感数据写入仓库。

停止条件：任何一项缺少现有接口、权限语义与规格冲突，或必须新增后端模型/迁移才能完成时，将对应任务标记 `[!]` 并请求产品确认。其余无冲突切片可以继续。

兼容性确认条件：核验中发现当前 Mobile 已实现、但原型或规格未展示的能力时，默认保留；任何删除、降级或交互改变必须先向产品确认并记录结论。

## 阶段 1：壳与平台事实

依赖：阶段 0 的权限、许可、账号偏好事实通过。

### 可用性与权限

- [x] `P1-1` 定义 Mobile 模块键、固定顺序、许可/菜单/operation 映射的强类型配置。
- [x] `P1-2` 实现静态菜单、用户菜单、自定义菜单和 client 许可的请求 adapter。
- [x] `P1-3` 实现平台可用性深模块：父级权限、非菜单项、失败关闭、刷新与并发请求防旧结果回写。
- [x] `P1-4` 实现按账号隔离的最近一级 Tab 记忆；登出或账号切换不得串值。
- [x] `P1-5` 为模块根页和子路由实现统一守卫与安全回退。

### 壳与导航

- [x] `P1-6` 将 `MobileTabShell` 扩展为五个候选 Tab，并只渲染平台可用性模块给出的可见项。
- [x] `P1-7` 修改根路由和登录后跳转，使用安全根页解析，不再固定 `/workbench`。
- [x] `P1-8` 扩展原生返回手势路由配置，覆盖新增详情；保证临时浮层先关闭。
- [x] `P1-9` 添加一级根页返回栈边界，验证 Tab 切换不会通过右滑穿回上一模块详情。
- [x] `P1-10` 对 `/conversation` 增加显式回归断言，保持抽屉手势且不启用原生返回。

### 语言与时区

- [x] `P1-11` 将 timezone 纳入登录用户类型和认证状态规范化。
- [x] `P1-12` 实现账号偏好深模块及统一时间格式化接口，保留语言和时区更新能力。
- [x] `P1-13` 迁移现有 OpsPilot 会话、历史和搜索时间到统一格式化接口。
- [x] `P1-14` 保留主题本地存储行为，验证账号偏好请求不读写主题。

阶段门槛：无权内容不闪现；根路由、登录、Tab、直达守卫、失权回退和账号时区测试通过后，才合并业务页面。

阶段验证（2026-08-03）：`pnpm lint`、`pnpm type-check`、`pnpm test`、`pnpm build:h5` 均通过；`pnpm build:tauri` 在未注入地址时按预期被环境守卫拒绝，随后以仅对命令生效的 `NEXT_PUBLIC_API_URL=http://localhost:8000` 完成静态产物构建。未写入环境文件，正式包仍需注入真实地址。

## 阶段 2：待办纵向切片

依赖：平台可用性、统一时间、导航父页。

- [x] `T2-1` 在待办模块内定义告警列表项、详情、事件、操作记录、级别和状态的领域类型，不复用原型 mock 类型。
- [x] `T2-2` 实现告警请求 adapter 和三个预设筛选构造器；高优先级取现有级别接口按 `level_id` 升序的第一个有效等级，保留 `global_config-View` 鉴权和局部 403 状态。
- [x] `T2-3` 实现待办根页：互斥预设视图、20 条分页、倒序、下拉刷新、触底加载和卡片字段。
- [x] `T2-4` 实现模块内搜索页，支持标题、内容、告警 ID，保留筛选和返回状态。
- [x] `T2-5` 实现告警详情摘要、事件和变更记录，统一按账号时区显示；变更记录沿用现有日志接口和 `operation_log-View`，403 不阻断其他详情区段。
- [x] `T2-6` 实现单条分派、认领、转派、关闭及 `Alarms-Edit` 可见性。
- [x] `T2-7` 处理重复提交、服务端并发冲突、403/404、刷新失败与部分数据失败。
- [x] `T2-8` 验证详情中不存在监控、资产、日志跳转和批量/静默等越界能力。

阶段门槛：四个状态动作通过服务端权限和状态校验；三个预设筛选验收通过；无权限用户只读。

阶段验证（2026-08-03）：新增 `test:todo` 覆盖三个预设筛选、第一个高优先级等级、搜索字段、分页去重、轻处置状态机、现有接口复用和无跨模块跳转；`pnpm lint`、`pnpm type-check`、`pnpm test`、`pnpm build:h5` 均通过，并以命令级本地占位地址完成 `pnpm build:tauri` 静态构建。Server 告警筛选、视图、级别的定向测试为 `48 passed`；pytest 在测试完成后的 coverage/GC 清理阶段长时间无输出，人工中断，因此进程退出码为 130，原始输出已保留。未新增 Server/Web 接口、权限、模型或迁移。

## 阶段 3：监控纵向切片

依赖：平台可用性、统一时间、导航父页。可与阶段 2、4 在不同开发人之间并行。

- [x] `M3-1` 定义监控分类、对象、实例、有效插件、指标分组和时序结果类型。
- [x] `M3-2` 实现监控请求 adapter；实例列表和搜索始终要求 `objectId`。
- [x] `M3-3` 实现“全部实例/最近查看”互斥入口；最近查看读写本机 `localStorage`（见 spec 架构约束，不写 Server）。
- [x] `M3-4` 实现 Mobile 可展开对象树，过滤不可见对象并显示真实实例数。
- [x] `M3-5` 实现选定对象下的实例列表、对象内搜索、分页和下拉刷新。
- [x] `M3-6` 实现实例详情头部、有效插件选择和最近 15 分钟默认区间。
- [x] `M3-7` 按 Web 指标分组动态展示单位和趋势剪影（小卡不展示当前值；大图 Sheet 呈现数值），按可视区域懒加载；单位语义对齐 Web `findUnitNameById`。详情为短时查看，不强制整页下拉刷新（切时间范围 / 失败重试 / 再次进入即可）。
- [x] `M3-8` 区分无指标配置与区间无数据；自动刷新保持默认关闭。
- [x] `M3-9` 验证最近查看不写 Server（无 Monitor 表、`recent_views` API）；索引存 Mobile `localStorage`；也没有跨对象搜索或跨模块跳转。

阶段门槛：任意监控对象/插件配置变化后页面无需改代码；查询时间语义与 Web 对齐。

阶段验证（2026-08-04）：确认联调环境 500 的根因包含本地数据库漏执行仓库已有迁移，以及 VictoriaMetrics 地址未配置却阻断实例基础列表。已应用增量字段迁移 `0050_monitor_instance_summary_facts`、`0046_metric_view_query`，未执行会删除并重建策略模板表的 `0051_rebuild_policy_template`；未新增或修改迁移文件。Server 在实时状态/展示指标不可用时改为保留数据库实例并标记“暂无数据”，Web/Mobile 共用，不伪造健康状态或指标值。Mobile 同步修正分类字符串 ID、Web `display_type` 解析和根页滚动高度链，并取消列表不消费的 `add_metrics` 请求。当前本地 Default 组织增加 3 条可单独识别的 `MOBILE-DEMO-HOST-*` 验收实例；真实浏览器确认列表接口 200、三条实例可见、详情有效插件接口 200，且详情准确展示“当前实例未配置指标”。Server 降级单测 `2 passed`；共享 `test_bklite` 的既有约束漂移仍需由 Server 迁移链单独收口。

## 阶段 4：资产纵向切片

依赖：平台可用性、统一时间、导航父页。可与阶段 2、3 在不同开发人之间并行。

- [x] `A4-1` 定义分类、模型、实例、字段组、全文搜索统计和关注配置类型。
- [x] `A4-2` 实现 CMDB 请求 adapter 和关注配置 adapter，不复制 Web 浏览器本地搜索历史。
- [x] `A4-3` 实现“我关注的/全部资产”互斥入口，“我关注的”为默认页。
- [x] `A4-4` 实现关注配置规范化、100 条限制、时间倒序、失权/删除过滤和部分模型失败容错。
- [x] `A4-5` 实现分类到模型的单列展开树、模型实例数和模型实例列表。
- [x] `A4-6` 实现全局搜索两阶段流程、模型命中数选择和默认关闭的精确匹配。
- [x] `A4-7` 实现由字段分组元数据驱动的只读资产详情和关注/取消关注。
- [x] `A4-8` 处理关注更新失败回滚、并发覆盖风险、403/404 和列表刷新。
- [x] `A4-9` 验证无 `search` 权限仍可浏览资产但不能进入全局搜索；详情没有桌面端越界能力。

阶段门槛：Web/Mobile 关注结果双向可见；任意模型字段变化无需 Mobile 硬编码适配。

阶段验证（2026-08-04）：使用当前账号真实关注配置完成 7 条关注资产列表、资产详情和动态字段组联调；搜索入口跟随 Web 独立 `search` 菜单权限。资产首页按 Web CMDB 的“搜索—关注资产—分类入口”层级重构，Mobile 分类仍保持已确认的单列展开树；首页、模型列表和搜索结果统一使用真实模型、组织、IP 与数量，不新增最近变更、统计推断或展示硬编码。完成 320/375px 下关注列表、全部分类与展开态视觉检查。

## 阶段 5：智能应用与我的回归补齐

依赖：阶段 1；可以在业务切片期间持续进行，最终统一收口。

- [x] `R5-1` 将现有工作台接入动态五 Tab 壳，不改变应用分类、搜索和会话请求契约。
- [x] `R5-2` 保持现有会话缓存、恢复、历史抽屉和消息流行为。
- [x] `R5-3` 原型模拟“在线”状态和本地“接收通知”不得成为新能力的验收依赖；本变更不顺手扩大清理范围。
- [x] `R5-4` “我的”展示服务端真实显示名、用户名、域、组织和角色。
- [x] `R5-5` 保留并对齐显示名、语言和时区更新；邮箱只读，不增加密码/邮箱修改入口。
- [x] `R5-6` 回归主题、H5/Tauri 退出登录、语言快捷入口与账号详情使用同一偏好源。

阶段门槛：现有 OpsPilot 核心流程测试全绿；偏好跨 Web/Mobile 刷新可见；主题仍仅本机生效。

阶段验证（2026-08-04）：工作台、搜索、会话列表和“我的”统一接入共享加载/空态/错误反馈；“我的”改为读取 `/console_mgmt/get_user_info/` 的真实账号、组织和角色，不再以内联模拟值补位。保留现有账号详情中的显示名、语言、时区和只读邮箱能力，未新增在线状态、通知或密码入口。完整 Node 契约测试通过，其中会话与壳层 42 项回归覆盖缓存、流式消息、退出、主题边界和五 Tab 导航。

## 阶段 6：综合质量与发布门禁

- [ ] `Q6-1` 补齐中英文文案，检查长标题、资源名、ID、主机名和空值展示。
- [ ] `Q6-2` 检查 320px、375–430px、亮/暗色、安全区、触摸目标、焦点和状态非纯颜色表达。
- [ ] `Q6-3` 覆盖 loading、empty、search empty、refresh error、offline、403、404、permission loading/error。
- [x] `Q6-4` 执行 Mobile 静态检查与核心测试：`pnpm lint`、`pnpm type-check`、`pnpm test`。
- [ ] `Q6-5` 执行 `pnpm build:h5` 与 `pnpm build:tauri`；原生工具链不可用时保留原始失败证据。
- [ ] `Q6-6` H5 与 Tauri 各完成登录、五 Tab 可见性、详情返回、刷新和退出冒烟。
- [ ] `Q6-7` 在 iOS/WKWebView 或等价可验证环境人工检查边缘返回；会话页单独检查抽屉手势。
- [ ] `Q6-8` 以 `spec.md` 全部验收场景做产品走查，记录延期项仍只有占位而无隐藏实现。

## 依赖与并行顺序

```text
阶段 0 事实核验
        |
        v
阶段 1 壳与平台事实
   |        |        |
   v        v        v
阶段 2    阶段 3    阶段 4
 待办      监控      资产
   \        |        /
    \       v       /
     阶段 5 回归收口
             |
             v
     阶段 6 综合质量
```

- 关键路径是 `事实核验 -> 平台事实 -> 任一业务纵向切片 -> 综合质量`。
- 待办、监控、资产只在阶段 1 接口稳定后并行，避免各自复制权限、时间和导航实现。
- 第一轮可演示版本建议按“平台壳 + 待办”形成完整纵向闭环；监控和资产随后并行接入同一壳。

## 预期文件影响

以下是模块落点建议，不是要求预先创建所有目录：

- `mobile/src/platform/availability/`：平台可用性模块与 HTTP adapter。
- `mobile/src/platform/preferences/`：账号偏好和时间格式化模块。
- `mobile/src/navigation/`、`mobile/src/components/mobile-tab-shell/`：壳与返回接口。
- `mobile/src/features/todo/`、`monitor/`、`assets/`：各业务实现与内部类型。
- `mobile/src/app/`：保持薄路由页面，只组装对应业务模块。
- `mobile/src/api/`：仅保留真正跨调用方的传输工具；不要把各业务的响应解析堆进通用 request。
- `mobile/src/locales/zh.json`、`mobile/src/locales/en.json`：同步新增文案。
- `mobile/scripts/` 或适合当前测试体系的位置：行为契约与核心流程测试。

只有出现经产品确认的现有接口缺口时，才新增 Server/Web 变更任务；届时必须单独补充服务端权限、数据范围、测试和是否迁移的影响说明。

## 每个任务的完成定义

一个任务只有同时满足以下条件才能标记 `[x]`：

1. 用户可观察行为符合 `spec.md`，没有以原型 mock 数据替代真实事实。
2. 权限、许可、账号范围和错误状态在模块接口处有覆盖。
3. 新逻辑有与风险相称的自动化验证，且测试通过模块接口观察结果，不依赖实现内部状态。
4. 中文、英文、亮色、暗色和适用屏宽已检查。
5. 没有跨模块入口、未授权直达、Mobile 专属业务模型或未确认的接口绕行。
6. 新鲜执行与影响范围匹配的 lint、类型检查、测试和构建，并在交付说明中记录结果。
