DEBUG=False
SECRET_KEY=7297613c-de40-477a-b13c-f86b3cc3cb99
JWT_ALGORITHM=HS256
INSTALL_APPS=system_mgmt

# 管理员引导凭据优先使用显式值，其次读取部署控制面挂载的 Secret 文件；均未配置时使用默认密码 password。
# 存量 admin 保持不变；需要迁移默认密码时，先配置受管凭据，再显式开启迁移。
BK_INIT_ADMIN_PASSWORD=
BK_INIT_ADMIN_PASSWORD_FILE=
BK_INIT_ADMIN_PASSWORD_MIGRATE_EXISTING=false

DB_ENGINE=postgresql
DB_NAME=system_mgmt
DB_USER=postgres
DB_HOST=127.0.0.1
DB_PASSWORD=password
DB_PORT=5432

NATS_SERVERS=127.0.0.1:4222
NATS_USER=
NATS_PASSWORD=
NATS_NAMESPACE=bklite
CLIENT_ID=system-manager

IS_USE_CELERY=false
DEFAULT_GROUP_NAME=Guest
TOP_GROUP=Default
