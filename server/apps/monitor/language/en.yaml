monitor_object_type:
  OS: Operating System
  Web: Probe
  Database: Database
  Middleware: Middleware
  LLM Inference: LLM Inference
  Network Device: Network Device
  Hardware Device: Hardware Device
  Container Management: Container Management
  K8S: K8S
  VMWare: VMware
  Cloud: Cloud
  Tencent Cloud: Tencent Cloud
  Aliyun Cloud: Aliyun Cloud
  Other: Other

# Fallback display name when plugin i18n is missing: show TCP, not TCPPort
monitor_object:
  TCPPort: TCP

flow_onboarding_ui:
  accessAsset: Access Asset
  accessGuide: Access Guide
  accessComplete: Access Complete
  accessCompleteTitle: Access Complete
  accessCompleteDesc: Your Flow asset is ready. The platform will ingest incoming
    Flow traffic for this asset.
  accessCompleteSubDesc: You can view the asset list, onboard another asset, or return
    to the template list.
  newAsset: New Asset
  existingAsset: Existing Asset
  accessAssetDesc: New Asset creates a new Flow monitoring asset. Existing Asset binds
    this protocol to an existing monitoring asset.
  selectExistingAsset: Please select an existing Flow asset
  existingAssetDesc: Select an existing Flow asset to reuse its identity and organization
    ownership.
  cloudRegion: Cloud Region
  selectCloudRegion: Please select cloud region
  cloudRegionDesc: Select the cloud region where this asset belongs. The Flow listener
    endpoint will use that region.
  assetIp: Asset IP
  assetIpDesc: Enter the device source IP that sends Flow data. It must match the
    exporter source address.
  assetName: Asset Name
  assetNameDesc: Use a recognizable asset name, such as Core Switch - Beijing.
  organization: Organization
  organizationDesc: Only selected organizations can view and manage this Flow asset.
  fallbackSamplingRate: Fallback Sampling Rate
  fallbackSamplingRateDesc: Default 1000. Used when the exporter payload does not
    provide an effective sampling rate after normalization.
  assetTipTitle: Flow access prerequisites
  assetTipDesc: Create or select a monitoring asset for the selected protocol, and
    keep the asset IP consistent with the exporter source address.
  guideIntroTitle: Configure device export
  guideIntroDesc: Follow the device steps below, then run detection after traffic
    is exported to the listener endpoint.
  protocol: Protocol
  endpoint: Endpoint
  normalizationRule: Normalization and sampling rule
  deviceGuide: Device guide
  detectHintTitle: Detection
  detectHintDesc: After completing device configuration, trigger detection to confirm
    the platform has received recent Flow data.
  detectAccess: Detect Access
  detectSuccess: Access Successful
  detectSuccessDesc: Recent Flow data has been received for this asset. The wizard
    will continue automatically.
  detectFailed: No Data Detected
  detectFailedDesc: No recent Flow data was detected. Check the exporter address,
    protocol, and source IP, then try again.
  lastSeenAt: Last Seen At
  effectiveSamplingRate: Effective Sampling Rate
  samplingRateSource: Sampling Rate Source
  viewAssetList: View Asset List
  addAnotherAsset: Add Another Asset
  backToTemplateList: Back to Template List
