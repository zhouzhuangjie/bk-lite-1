# language: zh-CN
功能: 告警处理动作引擎
  作为运维平台
  为了在告警出现时自动触发处置动作并记录结果
  ActionEngine 必须实现从创建到回调的完整闭环，并支持手动重跑

  场景: 创建告警命中规则触发作业并回写成功
    假设 存在一条启用的作业规则监听创建事件且匹配级别为1
    并且 节点管理中存在主机
    当 一条级别为1的告警被创建并评估
    那么 应生成一条running的执行记录
    当 job_mgmt回调该作业为成功
    那么 执行记录状态变为成功

  场景: 手动重跑动作不受幂等限制
    假设 存在一条作业规则但匹配条件不命中该告警
    当 使用同一幂等键对该告警手动触发该规则两次
    那么 应只生成一条手动执行记录
