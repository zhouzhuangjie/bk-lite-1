# ================================== Windows Event Log Configuration ==================================
{#- Security - 安全日志（身份验证、授权、审计） -#}
{%- if security_enabled | default(true) %}
- name: Security
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if security_level %}
  level: {{ security_level }}
  {%- endif %}
  {%- if security_event_id %}
  event_id: {{ security_event_id }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
{%- endif %}
{#- System - 系统日志（硬件、驱动程序、系统服务） -#}
{%- if system_enabled | default(true) %}
- name: System
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if system_level %}
  level: {{ system_level }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
{%- endif %}
{#- Application - 应用程序日志 -#}
{%- if application_enabled | default(true) %}
- name: Application
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if application_level %}
  level: {{ application_level }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
{%- endif %}
{#- Sysmon - 系统监控（进程、网络、文件活动） -#}
{%- if sysmon_enabled | default(false) %}
- name: Microsoft-Windows-Sysmon/Operational
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if sysmon_level %}
  level: {{ sysmon_level }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
{%- endif %}
{#- PowerShell Operational - PowerShell 操作日志 -#}
{%- if powershell_enabled | default(false) %}
- name: Microsoft-Windows-PowerShell/Operational
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if powershell_level %}
  level: {{ powershell_level }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
- name: Windows PowerShell
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if powershell_level %}
  level: {{ powershell_level }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
{%- endif %}
{#- Windows Defender - 防病毒日志 -#}
{%- if defender_enabled | default(false) %}
- name: Microsoft-Windows-Windows Defender/Operational
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if defender_level %}
  level: {{ defender_level }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
{%- endif %}
{#- Task Scheduler - 计划任务日志 -#}
{%- if task_scheduler_enabled | default(false) %}
- name: Microsoft-Windows-TaskScheduler/Operational
  {%- if ignore_older %}
  ignore_older: {{ ignore_older }}
  {%- endif %}
  {%- if task_scheduler_level %}
  level: {{ task_scheduler_level }}
  {%- endif %}
  fields:
    collector: "Winlogbeat"
    collect_type: "winlogbeat"
    instance_id: "{{ instance_id | default('default') }}"
  fields_under_root: true
{%- endif %}
