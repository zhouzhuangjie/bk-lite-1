# language: zh-CN
功能: 补丁目标数据权限
  作为补丁管理员
  我希望补丁目标列表复用平台统一的组织与数据规则
  以便补丁管理与作业平台保持一致

  场景: 正常路径 - 浏览器会话用户查看团队授权目标
    假设 当前用户属于团队 1
    并且 当前团队选择为 1
    并且 存在目标"apt-host"属于团队 1
    并且 存在目标"yum-host"属于团队 1
    并且 数据权限授予整个团队 1
    当 用户查询补丁目标列表
    那么 请求应当成功
    并且 返回目标名称应为"apt-host,yum-host"

  场景: 边界情况 - 未选择当前团队时拒绝访问
    假设 当前用户属于团队 1
    并且 未选择当前团队
    并且 存在目标"dnf-host"属于团队 1
    并且 数据权限授予整个团队 1
    当 用户查询补丁目标列表
    那么 请求应当被拒绝

  场景: 边界情况 - 不得选择用户不属于的团队
    假设 当前用户属于团队 1
    并且 当前团队选择为 2
    并且 存在目标"foreign-host"属于团队 2
    并且 数据权限授予整个团队 2
    当 用户查询补丁目标列表
    那么 请求应当被拒绝

  场景: 边界情况 - 没有任何数据规则时返回空列表
    假设 当前用户属于团队 1
    并且 当前团队选择为 1
    并且 存在目标"unruled-host"属于团队 1
    并且 数据权限为空
    当 用户查询补丁目标列表
    那么 请求应当成功
    并且 返回目标列表应为空

  场景: 边界情况 - 包含子组织时可查看子组织授权目标
    假设 当前用户属于团队 1
    并且 当前团队选择为 1
    并且 当前团队包含子组织 2
    并且 存在目标"child-host"属于团队 2
    并且 数据权限授予整个团队 2
    当 用户查询补丁目标列表
    那么 请求应当成功
    并且 返回目标名称应为"child-host"

  场景: 边界情况 - 显式实例规则可授权指定目标
    假设 当前用户属于团队 1
    并且 当前团队选择为 1
    并且 存在目标"cross-team-host"属于团队 2
    并且 数据权限只授予目标"cross-team-host"
    当 用户查询补丁目标列表
    那么 请求应当成功
    并且 返回目标名称应为"cross-team-host"
