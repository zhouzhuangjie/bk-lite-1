name: rabbitmq
version: 2.0.0
category: middleware
metadata:
  display_name: RabbitMQ 消息队列
  description: RabbitMQ 消息队列采集插件，支持脚本方式采集
  author: WeOps
  tags:
  - middleware
  - rabbitmq
  - mq
  cloud_protocol: false
  model_id: rabbitmq
executors:
  job:
    type: job
    execution_mode: remote
    capacity_group: remote_job
    scripts:
      linux: plugins/inputs/rabbitmq/rabbitmq_default_discover.sh
    collector:
      module: plugins.script_executor
      class: SSHPlugin
    default_script: linux
    target_policy:
      mode: remote_channel
default_executor: job
