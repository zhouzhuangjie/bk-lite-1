name: etcd
version: 2.0.0
category: middleware
metadata:
  display_name: Etcd 分布式存储
  description: Etcd 分布式键值存储采集插件，支持脚本方式采集
  author: WeOps
  tags:
  - middleware
  - etcd
  cloud_protocol: false
  model_id: etcd
executors:
  job:
    type: job
    execution_mode: remote
    capacity_group: remote_job
    scripts:
      linux: plugins/inputs/etcd/etcd_default_discover.sh
    collector:
      module: plugins.script_executor
      class: SSHPlugin
    default_script: linux
    target_policy:
      mode: remote_channel
default_executor: job
